{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "dan001",
  "password": "xr-project_0",
  "database": "postgresql",
  "entities": ["src/**/*.entity.ts"],
  "synchronize": true,
  "logging": true,
  "migrations": ["src/migrations/*.ts"],
  "cli": {
    "migrationsDir": "src/migrations"
  }
}
